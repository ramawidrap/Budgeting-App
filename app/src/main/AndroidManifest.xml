<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sibi.budgetingapp">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.DetailBudgetActivity"></activity>
        <activity android:name=".ui.BudgetActivity" />
        <activity android:name=".ui.expense.ExpenseEditActivity" />
        <activity android:name=".ui.income.IncomeEditActivity" />
        <activity
            android:name=".ui.login.EditActivity"
            android:label="@string/title_activity_edit" />
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>